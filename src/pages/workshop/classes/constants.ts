export const CLASS_LEVELS = Array.from({ length: 20 }, (_, index) => index + 1);

export const EMPTY_CLASS_TABLE_VALUE = 0;

export const INFINITE_CLASS_TABLE_VALUE = -1;

export const MAX_CLASS_TABLE_VALUE = 127;
